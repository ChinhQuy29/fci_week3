services:
  api:
    image: quychinh2004/week3-api:latest
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./app.db
      DEBUG: "true"
    volumes:
      - ./app.db:/app/app.db
